# Gemmra — Dependencies
# Install in this exact order on AMD MI300X

# ⚠️ CRITICAL: Install bitsandbytes pre-release FIRST (see src/setup/install.sh)
# pip install --force-reinstall --no-cache-dir --no-deps \
#   "https://github.com/bitsandbytes-foundation/bitsandbytes/releases/download/continuous-release_main/bitsandbytes-1.33.7.preview-py3-none-manylinux_2_24_x86_64.whl"

# Core ML
# ⚠️ CRITICAL: Default PyPI torch is CPU-only! For AMD MI300X, install ROCm build:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.2
# Or just run: bash src/setup/install.sh (it handles this automatically)
torch>=2.1.0
unsloth[amd]

# Training
trl>=0.9.0
datasets>=2.16.0
peft>=0.8.0
accelerate>=0.25.0
transformers>=4.40.0

# Data Pipeline
duckdb>=0.9.0
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0
requests>=2.31.0
lxml>=5.0.0
pyyaml>=6.0.0

# Demo
streamlit>=1.30.0
plotly>=5.18.0

# Evaluation
scikit-learn>=1.3.0
